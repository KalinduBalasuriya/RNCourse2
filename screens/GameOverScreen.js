import { Text, StyleSheet } from "react-native";

function GameOverScreen() {
    return <Text style={styles.container} >Game is Over</Text>
}
export default GameOverScreen;

const styles = StyleSheet.create({
    container : {
        paddingTop:24,
    }
}) 

       
        
